import { Meta, StoryObj } from '@storybook/react'
import { Home } from "./Home";

export default {
  title: 'Pages/Home',
  component: Home,
  args: {},
  argTypes: {},
} as Meta

export const Default: StoryObj = {}
